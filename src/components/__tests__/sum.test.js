import { sum } from "../sum"



// test('should calculate the sum of two numbers ', () => {
//     const result=sum(4,6);

//     //Assertion
//     expect(result).toBe(10)
  
// })

test('should calculate the sum of two numbers', () => {
    const result = sum(4, 6);
  
    // Assertion
    expect(result).toBe(10);
  });